# Automatically create a pull request when a hotfix/* or feature/* branch is pushed.
# Also labels PRs based on branch prefix.

name: Create pull request

on:
  push:
    branches:
      - "hotfix/**"
      - "feature/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  create-pr:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Create pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash .github/scripts/auto-pr/create-pr.sh "${{ github.ref_name }}"

      - name: Label PR by branch prefix
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash .github/scripts/auto-pr/label-pr.sh "${{ github.ref_name }}"
