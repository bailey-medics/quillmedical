generation: 1
forces_reload: false
change: "new-required-request-property POST /api/sites/{site_id}/staff added the new required request property `user_id`"
reason: 'user_id was already runtime-required (route raised 422 with detail "user_id and role are required" if the key was missing from the body). No behaviour change for any existing client.'
