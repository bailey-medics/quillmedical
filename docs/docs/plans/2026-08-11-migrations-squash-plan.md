# Alembic migrations squash plan

**Date:** 2026-08-11
**Status:** Agreed; awaiting merge of the migration-safety work (PR #362) to
`main`, then implementation on a fresh branch
**Scope:** `backend/alembic/` — collapse the entire core-DB migration history
into a single baseline migration, pre-launch, while there is no live data
**Parent:** [Alembic review and migration safety plan](2026-08-09-alembic-review-plan.md)
(this plan delivers the squash that plan's item 17 defers here)

## Why we are doing this

The core DB carries 35 linear migrations accumulated during pre-launch
development. Several are grandfathered debt: seven have empty descriptions /
bare `<rev>_.py` slugs, some predate the reversibility and NOT-NULL rules, and
one (`b66133f32f7b`) exists only to repair index-rename drift from an earlier
table rename. `backend/scripts/check_migrations.py` manages this debt with an
**allow-list** of the 35 legacy revisions rather than fixing it.

Because there is **no live or irreplaceable data anywhere**, we can retire all
of that debt at once by replacing the whole chain with a single baseline
migration autogenerated from the current models. This is the canonical
"collapse migrations before go-live" cleanup.

### Preconditions (all confirmed)

- **No production environment exists.** Production is a later, gated milestone.
- **No staging environment exists.**
- **Teaching is disposable** — the only data is throwaway developer play data,
  safe to drop and recreate.
- **Single developer** — the one-time "drop your core DB and re-migrate" cost
  falls on one machine; no team coordination is required.
- **Core DB only.** `env.py` migrates the core DB alone. HAPI FHIR and EHRbase
  own their schemas and are entirely unaffected by this squash.

## What the squash supersedes

Once the baseline lands, several parent-plan items and existing mechanisms
become unnecessary — the baseline is a single, standards-compliant migration,
so there is no legacy history left to grandfather or tidy:

- **`check_migrations.py` allow-list** — `ALLOWLISTED_REVISIONS` can be emptied.
  The baseline meets the full standard (real docstring, meaningful slug, real
  `downgrade()`), so nothing needs grandfathering. The five checks still guard
  every future migration.
- **Parent item 6 (backfill empty descriptions)** — moot; the seven
  empty-description files cease to exist.
- **Parent item 7 (typing-style reformat of `alembic/versions`)** — reduced to
  a single generated file, formatted on creation.
- **Parent item 8 (retroactive date-prefixed filenames)** — only the baseline
  remains; still worth enabling `file_template` in `alembic.ini` for _future_
  migrations, but there is nothing legacy left to rename.
- **The `b66133f32f7b` index-rename migration** — folds into the baseline; the
  index is simply created with its correct British-spelling name from the start.

## Part 1 — Implementation steps

Ordered as an implementation sequence. Each is a discrete, reviewable unit;
present each for human review before commit (review gate per
`follow-the-plan-document.prompt.md`). Nothing is committed or pushed except via
the `/crp` prompt.

### 1. Branch from a freshly merged `main`

- [x] After PR #362 merges to `main`, create `feature/squash-migrations` off the
      updated `main`.

The squash must build on the merged migration-safety tooling
(`check_migrations.py`, the `just migrate -e` fix, `backend.instructions.md`),
which stays valuable going forward.

### 2. Regenerate the schema baseline

- [x] Delete every file in `backend/alembic/versions/`.
- [x] Against a **freshly created, empty** core DB, generate one baseline:
      `alembic revision --autogenerate -m "initial baseline schema"`
      (yielded `878bc9300d4f_initial_baseline_schema.py`).
- [x] Give the baseline a real module docstring and keep the **autogenerated**
      `downgrade()` — a full drop-all that mirrors `upgrade()` and returns the DB
      to empty (`base`). There is nothing below the baseline, so this is the
      complete, correct reverse, not a special or irreversible case. It must pass
      `check_migrations.py` on its own merits.

The baseline is autogenerated from `Base.metadata` + the teaching models
(exactly what `env.py` targets), so it reproduces the current schema precisely.

### 3. Verify no drift on a fresh DB

- [x] `alembic upgrade head` on a fresh core DB, then `alembic check` — must
      report **no** drift (the baseline matches the models exactly). Confirmed:
      `No new upgrade operations detected.`
- [x] Confirm the chain is a single base == head (the baseline is both).
      Confirmed: `<base> -> 878bc9300d4f (head)`.

### 4. Retire the grandfathering

- [x] Empty `ALLOWLISTED_REVISIONS` in `backend/scripts/check_migrations.py`
      and update its unit tests.
- [x] Run `check_migrations.py --all` — the baseline passes with no allow-list.

### 5. Update the documented rules and reasoning

- [x] In `.github/instructions/backend.instructions.md`: replace the "existing 35
      revisions are grandfathered via an allow-list" wording; repoint or remove
      the now-stale canonical-example citations (`197844c56085`, `b66133f32f7b`)
      that no longer exist as standalone revisions.
- [x] Update the parent plan's item 17 to reflect the completed squash. The
      historical migration-count analysis in that plan is left intact on purpose
      (rewriting a point-in-time record would falsify it).

### 6. Recreate local / teaching databases

- [x] Drop and recreate the local core DB, then `alembic upgrade head` to the new
      baseline. Done as a one-off reset (no permanent `just` recipe — it is a
      single pre-launch operation), verified end-to-end against the local stack.
- [ ] Recreate the teaching core DB the same way (throwaway data — no migration
      of content needed).

Any existing core DB is stamped at the old `head`, which no longer exists after
the squash, so it must be dropped and rebuilt rather than upgraded.

### 7. Confirm the unit suite is unaffected

- [x] Run `just ub`. The backend unit tests build their schema on SQLite via
      `Base.metadata.create_all()`, **not** via Alembic migrations, so squashing
      the migration history should leave them entirely green. Confirmed: full
      suite passes.

### 8. Finish the `auth` → `core` naming consistency

The database was renamed to `quill_core` and the env vars to `CORE_DB_*` in an
earlier effort (see `2026-05-26-db-rename-plan.md`), and the session module is
already `backend/app/db/core_db.py`. What remains are the low-level dev/CI and
config identifiers still spelled `auth`. Since we are rebuilding every DB from
empty in this window, complete the rename now so nothing but historical records
keeps the old name:

- [ ] **Local dev** (`compose.dev.yml`): `postgres-auth` service → `postgres-core`;
      container `quill_postgres_auth` → `quill_postgres_core`; `POSTGRES_USER:
    auth_user` → `core_user`; volume `postgres_auth_data` → `postgres_core_data`;
      the `depends_on` and healthcheck references to match.
- [ ] **CI** (`compose.ci.yml`): the same set (`ci_postgres_auth` →
      `ci_postgres_core`, `auth_user` → `core_user`, `postgres-auth` →
      `postgres-core`), plus the explanatory comment.
- [ ] **Backend config** (`backend/.env`, `backend/app/config.py` defaults):
      `CORE_DB_USER` default `auth_user` → `core_user`; `CORE_DB_HOST` default
      `postgres-auth` → `postgres-core`.
- [ ] **Docs refresh**: repoint the stale `AUTH_DB_*` / `postgres-auth` references
      in `docs/docs/infrastructure/gcp.md` and
      `docs/docs/backend/three-database-migration.md` to `CORE_DB_*` /
      `postgres-core`.

**Leave historical records untouched** — the `2026-05-26-db-rename-plan.md` and
everything under `safety/hazard-analysis/outputs/**` (and the `Hazard-00xx.md`
files) are point-in-time analysis; rewriting them would falsify history. Note the
deployed **GCP** Cloud SQL user is already `quill` (not `auth_user`), so the user
rename touches only local dev, CI, and the config defaults. The GCP-side `auth`
names (the `auth-db-password` secret and the `quill-auth-{env}` instance) are
handled in step 9.

### 9. Rename the Cloud SQL instance and reset the deployed GCP core database(s)

The core DB runs on **Cloud SQL for PostgreSQL**, private-IP only, reached by
Cloud Run over the Virtual Private Cloud (VPC) connector. Migrations run from the **serving backend
container's entrypoint** (`alembic upgrade head`, 5 retries) — there is no
separate migration job. An already-deployed DB is stamped at the old head, which
the squash deletes, so the next deploy's `upgrade head` would fail (unknown
revision) **and** the baseline's `create_table`s would clash with the tables
already present. The deployed DB must be rebuilt empty, not upgraded.

The **top deployed environment is teaching** (the staging-equivalent); there is
no live clinical production yet (production deploys are gated off via
`ENABLE_PRODUCTION_DEPLOY`). So teaching leads; production is the identical
procedure **if and when** it is ever provisioned.

**Fold in the instance rename.** The database was renamed to `quill_core`, but
the Cloud SQL _instance_ is still built as `quill-auth-{env}` from
`instance_name = "auth"` in `infra/main.tf`. Since we are rebuilding these
databases anyway with no data to lose, rename the instance to `quill-core-{env}`
in the same window:

- [ ] In `infra/main.tf`, change the core module's `instance_name` from `"auth"`
      to `"core"` (yielding `quill-core-teaching` / `quill-core-prod`). For
      consistency, also rename the `auth-db-password` secret to
      `core-db-password` (Secret Manager + the module's `db_password_secret_id`),
      and update the `quill-auth-{{env}}` reference in the `Justfile`
      (`build-admin` recipe) to `quill-core-{{env}}`.
- [ ] Apply the Terraform change. Cloud SQL instances cannot be renamed in
      place, so this **replaces** the instance (destroy + create) — acceptable
      now precisely because there is no data. Note `deletion_protection = true`
      on the module for `prod`; lift it if it blocks a prod replacement.

**Rebuild and verify (teaching first).** The replacement above already yields a
fresh, empty `quill_core`, so no separate drop is needed — just ship the
baseline:

- [ ] **Deploy the backend revision carrying the squashed baseline** to
      **teaching** (`quill-core-teaching`, project `quill-medical-teaching`). The
      entrypoint's `alembic upgrade head` applies the single baseline to the
      empty DB, building the whole schema fresh; the revision goes Ready only if
      that succeeds.
- [ ] **Verify.** Confirm the revision is serving and `alembic current` reports
      the baseline revision (e.g. via the `quill-admin-teaching` Cloud Run job).
- [ ] **Production, if/when provisioned:** repeat identically against
      `quill-core-prod` (project `quill-medical-production`). Nothing to do today
      while prod is gated off.

If you ever need to reset a deployed DB **without** replacing the instance, drop
and recreate the database directly with Cloud SQL control-plane commands (they
work despite the private-only IP; the instance and `quill` user persist):

```sh
gcloud sql databases delete quill_core \
  --instance=quill-core-teaching --project=quill-medical-teaching
gcloud sql databases create quill_core \
  --instance=quill-core-teaching --project=quill-medical-teaching
```

**Post-launch contrast (not now):** once an environment holds real data it is
**never** dropped. A future squash would instead `alembic stamp <baseline>` the
live DB so its `alembic_version` points at the new baseline without touching the
schema. This whole step is only safe inside the current no-data window.

## Part 2 — Risks and mitigations

- **A stamped DB survives the squash.** Any core DB still stamped at the old
  head would break on the next `alembic upgrade head` (the recorded revision
  no longer exists). _Mitigation:_ steps 6 and 9 rebuild every core DB from
  empty — local, teaching (the top deployed environment), and production if and
  when it is ever provisioned.
- **Baseline drift from the live models.** If the baseline is generated against
  a non-empty or stale DB, autogenerate could miss or duplicate objects.
  _Mitigation:_ step 2 generates against a **freshly created empty** DB; step 3
  proves `alembic check` is clean before the branch is offered for review.
- **Loss of schema-evolution history.** The squash discards the per-migration
  audit trail. _Mitigation:_ pre-launch this is negligible, and this plan plus
  the parent plan remain the historical record. This is precisely why now — not
  post-launch — is the moment to do it.

## Part 3 — Tests

- [ ] **Fresh-DB migrate + check** — `alembic upgrade head` then `alembic check`
      on an empty core DB reports no drift (the baseline == the models).
- [ ] **Checker passes without an allow-list** — `check_migrations.py --all`
      exits 0 against the single baseline with `ALLOWLISTED_REVISIONS` empty.
- [ ] **Checker tests updated** — the allow-list-specific unit tests are removed
      or rewritten; the five per-migration checks still have passing/failing
      fixtures.
- [ ] **Unit suite green** — `just ub` passes unchanged (SQLite `create_all`
      path is independent of migration history).
- [ ] **Drift gate green (parent item 4)** — once the autogenerate-drift CI job
      lands, it passes immediately against the baseline.
